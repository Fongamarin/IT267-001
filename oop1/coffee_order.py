from ast import Num
from numbers import Integral


class coffee :
    def __init__(self,manu_trpy,num,size,price = "Coffe")
        self.manu = manu_trpy
        self.num = num
        self.size = size
        self.price = price

        def check_manu(manu)
        manu = manu.upper()
        if manu == "cm":
            return "cafe mocha"
        if manu == "cp":
            return "capuchino"
        if manu == ""

